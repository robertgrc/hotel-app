import { Grid, Typography } from "@mui/material";
import * as React from "react";

export default function Deposits() {
  return (
    <React.Fragment>
      <Grid container>
        <Grid>
          <Typography color="secondary">Ganancias</Typography>
        </Grid>
      </Grid>
    </React.Fragment>
  );
}
