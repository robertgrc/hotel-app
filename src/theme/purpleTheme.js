import { createTheme } from "@mui/material";

export const purpleTheme = createTheme({
  palette: {
    primary: {
      main: "#56ca00",
    },
    secondary: {
      main: "#9155fd",
    },
    error: {
      main: "#f5f5f5",
    },
  },
  status: {
    danger: "#f5f5f5",
  },
});
